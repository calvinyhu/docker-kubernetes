language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t calvinyhu/docker-kubernetes -f ./section6/frontend/Dockerfile.dev ./section6/frontend

script:
  - docker run -e CI=true calvinyhu/docker-kubernetes npm t
